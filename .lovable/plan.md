

## Plan: Update Favicon to iBuildPro Logo

Copy the uploaded logo to `public/favicon.png` (replacing the existing one) so the existing `<link rel="icon" type="image/png" href="/favicon.png">` in `index.html` picks it up automatically.

**Steps:**
1. Copy `user-uploads://ibuildPro_Logo.png` to `public/favicon.png`

No code changes needed — `index.html` already references `/favicon.png`.

